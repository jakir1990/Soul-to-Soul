{% extends 'base.html.twig' %}

{% block body %}
    <h1>Pubs list</h1>

    <table>
        <thead>
            <tr>
                <th>Id</th>
                <th>Pubname</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for pub in pubs %}
            <tr>
                <td><a href="{{ path('pub_show', { 'id': pub.id }) }}">{{ pub.id }}</a></td>
                <td>{{ pub.pubName }}</td>
                <td>
                    <ul>
                        <li>
                            <a href="{{ path('pub_show', { 'id': pub.id }) }}">show</a>
                        </li>
                        <li>
                            <a href="{{ path('pub_edit', { 'id': pub.id }) }}">edit</a>
                        </li>
                    </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('pub_new') }}">Create a new pub</a>
        </li>
    </ul>
{% endblock %}
